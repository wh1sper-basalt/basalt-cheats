# GitHub Notes

- This is a standalone query-routing variant.
- Keep parity with `new-age/official` UI and database behavior.
- Differences must stay limited to URL strategy and educational compatibility requirements.

